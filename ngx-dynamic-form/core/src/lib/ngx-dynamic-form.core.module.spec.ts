import { NgxDynamicFormCoreModule } from './ngx-dynamic-form.core.module';

describe('CoreModule', () => {
  it('should work', () => {
    expect(new NgxDynamicFormCoreModule()).toBeDefined();
  });
});
